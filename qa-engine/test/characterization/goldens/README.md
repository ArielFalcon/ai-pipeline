# Historical goldens

These JSON files are frozen snapshots from the src/ → qa-engine characterization program.
No live test loads them: FixLoop characterization and shadow-comparison reconstruct the
same scenarios in code. Kept as documentation of the expected verdicts, not as a runtime
harness. Do not wire a new test to these files.
