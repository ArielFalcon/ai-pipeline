{
  "name": "@qayaba/web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Operator/value web dashboard for qayaba. Built to web/dist and served same-origin by the orchestrator at /app. Consumes @qayaba/sdk. Implementation is delegated — this package is the prepared slot.",
  "dependencies": {
    "@qayaba/sdk": "*"
  }
}
